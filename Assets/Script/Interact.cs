using UnityEngine;

public class Interact : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void InteractFactory(GameObject hitObj, GameObject pickupHintUI)
    {
        if (hitObj.CompareTag("Potato"))
        {

        }
        if (hitObj.CompareTag("Dog"))
        {

        }    
    }    
}
